if( !AjaxNotify{{ NOTIFYHASH }} ) {
    AjaxNotify{{ NOTIFYHASH }} = $.notify({
        title: 'Daten werden verarbeitet',
        message: '{{ NOTIFYLOADING }}'
    }, {
        z_index: 32768,
        showProgressbar: true,
        newest_on_top: true,
        type: 'info',
        delay: 0,
        placement: {
            from: 'top',
            align: 'right'
        },
        template:'<div data-notify="container" class="col-xs-11 col-sm-4 alert alert-{0}" role="alert">' +
            '<button type="button" aria-hidden="true" class="close" data-notify="dismiss">×</button>' +
            '<span data-notify="icon"><span class="loading-indicator-animate"></span>&nbsp;</span>' +
            '<span data-notify="title">{1}</span><br/>' +
            '<div class="progress" data-notify="progressbar" style="height: 3px; margin: 0;">' +
                '<div class="progress-bar progress-bar-{0}" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" style="width: 0%;"></div>' +
                '</div>' +
            '<span data-notify="message">{2}</span>' +
            '<a href="{3}" target="{4}" data-notify="url"></a>' +
            '</div>'
    });
} else {
    AjaxNotify{{ NOTIFYHASH }}.update({ message: '{{ NOTIFYLOADING }}' });
}
AjaxNotify{{ NOTIFYHASH }}.update({progress: {{ random(2) }}{{ random(9) }} });

var {{ RESPONSE }} = {{ CONTENT }};

{% for Receiver in ReceiverList %}
    {{ Receiver.getHandler }}
{% endfor %}

{{ NOTIFY }}

{{ CALLBACK }}
