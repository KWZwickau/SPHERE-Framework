(function($)
{
    'use strict';
    $.fn.ModCheckBox = function(
//    options
    )
    {

        // This is the easiest way to have default options.
//        var settings = $.extend( {
        // These are the defaults.
//        }, options );

        // script goes here
        this.iCheck();
        return this;

    };

}(jQuery));
